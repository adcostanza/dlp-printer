PShape bot;
int num = 0;
int i = 0;
void setup() {
  size(640, 360);
  background(0);
} 

void draw(){
  
  bot = loadShape("job/layer"+i+".svg");
  shape(bot, 110, 90);  // Draw at coordinate (110, 90) at size 100 x 100
  delay(300);
  i=i+1;
}